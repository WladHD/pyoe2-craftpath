# Python dev requirements for the local gate (tests, example notebooks and
# example scripts). Installed into backend/crates/pyoe2-craftpath/.venv by
# backend/scripts/setup_python_env.sh; the extension itself is built into the
# venv by maturin. Floors match the extras in pyoe2-craftpath's pyproject.toml.

# test suite (mirrors the [dev] + [client] extras)
pytest>=8
pytest-asyncio>=0.24
httpx>=0.27
websockets>=13
protobuf>=5.29

# example notebooks + scripts
pandas
matplotlib
requests

# notebook execution
nbconvert
nbclient
ipykernel
