{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "craftpath.serviceAccountName" . }}
  labels:
    {{- include "craftpath.labels" . | nindent 4 }}
{{- end }}
